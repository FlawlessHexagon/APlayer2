# Run with `dart run ffigen --config ffigen.yaml`.
name: AudioDspBindings
description: |
  Bindings for `src/audio_dsp.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/audio_dsp_bindings_generated.dart'
headers:
  entry-points:
    - 'src/audio_dsp.h'
  include-directives:
    - 'src/audio_dsp.h'
comments:
  style: any
  length: full
